subprojects {
    apply plugin: 'java-library'
    apply plugin: 'idea'

    group 'eu.scattering'
    version '1.0-SNAPSHOT'

    java {
        modularity.inferModulePath = true
    }

    repositories {
        mavenCentral()
    }
}