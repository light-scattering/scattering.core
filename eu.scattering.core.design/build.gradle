plugins {
    id 'java-library'
    id "com.vanniktech.maven.publish"
}

dependencies {
    api 'org.json:json:20231013'
}

// --- Clean ---

tasks.named('clean') {
    delete 'out'

    dependsOn('removeFromMavenLocal')
}